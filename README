诸法实相，缘起性空=====cTest=====Windows下编译运行方法：	方法 1.	Sublime+Run.bat	编译		Sublime自定义编译系统（单文件）		{			"cmd": ["C:\\MinGW\\bin\\mingw32-cc.exe", "$file", "-o", "main.exe"]		}		Sublime自定义编译系统（多文件），makefile同方法2中		{			"cmd": ["C:\\MinGW\\bin\\mingw32-make.exe"]		}	运行Run.bat		@echo off		main.exe		@echo.	%empty line%		@pause	方法 2. 批处理	MakeRun.bat		@echo off		C:\MinGW\bin\mingw32-make.exe		main.exe		@echo.	%empty line%		@pause	Makefile		SRCS = $(wildcard *.c)		OBJS = $(SRCS:.c = .o)		CC = mingw32-gcc		CCFLAGS = -g -Wall -O0		main : $(OBJS)			$(CC) $^ -o $@		%.o : %.c			$(CC) -c $< $(CCFLAGS)		clean:			rm *.oMac/Linux下编译运行方法：	//运行checkEnvironment检查环境，到当前目录下	用Sublime编辑代码文件	用GCC/Make编译代码：		gcc target.c –o target		make	运行target=====pyTest=====Windows下运行方法：	安装pythonxy	命令行/设置python解释器路径，在编辑器中运行脚本Mac/Linux下运行方法：	命令行